import React from 'react'; // eslint-disable-line no-unused-vars
import { Outlet } from 'react-router-dom';

const App = () => {
  return (
    <>
      <Outlet />
    </>
  );
};

export default App;
